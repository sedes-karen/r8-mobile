import { View, Text } from 'react-native';

export function ArtistPromosPlayerScreen() {
  return (
    <View style={{ flex: 1 }}>
      <Text>Edite la pantalla ArtistPromosPlayerScreen para cambiar esto</Text>
    </View>
  );
}
