import { View, Text } from 'react-native';

export function AuthPasswordResetScreen() {
  return (
    <View style={{ flex: 1 }}>
      <Text>Edite la pantalla AuthPasswordResetScreen para cambiar esto</Text>
    </View>
  );
}
