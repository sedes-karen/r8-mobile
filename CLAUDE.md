@AGENTS.md

## Claude Code
If asked to edit agent instructions, read and edit the `AGENTS.md` file instead of `CLAUDE.md`. Both files are automatically synchronized without requiring edits to `CLAUDE.md`.
